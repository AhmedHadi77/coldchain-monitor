# ColdChain Monitor — local development MQTT broker

# Listen for MQTT connections on the standard MQTT port.
listener 1883

# Allow local development clients to connect without credentials.
# IMPORTANT: Never use this setting in a production deployment.
allow_anonymous true

# Write broker logs to Docker's standard output.
log_dest stdout

# Keep MQTT messages only in memory for this first local setup.
persistence false
